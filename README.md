# boiler-room-plate

## backend
preparation:
```
cd backend
virtualenv .venv
. .venv/bin/activate
pip install -r requirements.txt

```

to start the backend:
```
cd backend
. .venv/bin/activate
./server.py 127.0.0.1 8080
```
